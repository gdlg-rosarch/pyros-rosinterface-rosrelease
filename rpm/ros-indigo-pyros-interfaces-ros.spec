Name:           ros-indigo-pyros-interfaces-ros
Version:        0.4.0
Release:        0%{?dist}
Summary:        ROS pyros_interfaces_ros package

Group:          Development/Libraries
License:        BSD
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-indigo-message-runtime >= 0.4.12
Requires:       ros-indigo-pyros-common >= 0.4.2
Requires:       ros-indigo-pyros-utils >= 0.1.3
Requires:       ros-indigo-rospy >= 1.11.19
Requires:       ros-indigo-std-msgs >= 0.5.9
BuildRequires:  ros-indigo-catkin >= 0.6.18
BuildRequires:  ros-indigo-catkin-pip >= 0.1.17
BuildRequires:  ros-indigo-message-generation >= 0.2.10
BuildRequires:  ros-indigo-pyros-common >= 0.4.2
BuildRequires:  ros-indigo-pyros-test >= 0.0.6
BuildRequires:  ros-indigo-pyros-utils >= 0.1.3
BuildRequires:  ros-indigo-roslint >= 0.10.0
BuildRequires:  ros-indigo-rospy >= 1.11.19
BuildRequires:  ros-indigo-rostest >= 1.11.19
BuildRequires:  ros-indigo-rostopic >= 1.11.19
BuildRequires:  ros-indigo-rosunit >= 1.11.12
BuildRequires:  ros-indigo-std-msgs >= 0.5.9

%description
Dynamic ROS interface for Pyros

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/indigo" \
        -DCMAKE_PREFIX_PATH="/opt/ros/indigo" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/indigo

%changelog
* Mon Apr 24 2017 AlexV <asmodehn@gmail.com> - 0.4.0-0
- Autogenerated by Bloom

